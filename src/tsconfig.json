{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        // Nothing more to add to the defaults
        "outDir": "../lib",
    },
    "include": [
        "**/*.ts"
    ],
    "exclude":[
        "node_modules"
    ]
}