{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        // Nothing more to add to the defaults
        "declaration": true,
        "declarationDir": "../dts",
        "outDir": "../lib",
    },
    "include": [
        "index.ts"
    ],
    "exclude":[
        "node_modules"
    ]
}